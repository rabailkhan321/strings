#include<stdio.h>
#include<string.h>
int main()
{

    char name[40];
    int count=0, i=0;
    printf("please enter your name\n");
    gets(name);
    while(name[i]!='\0')
    {
        count++;
        i++;

    }
    printf("the length of the string is %d", count);
}
